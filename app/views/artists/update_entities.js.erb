var id = "<%= @entity.class.name.downcase %>-<%=@entity.id%>"
$.modal.close();
$('#add-work-entity-modal').empty();
$('#entities-wrapper').append("<div class='col-md-3 reduced-padding' id="+id+"><%= escape_javascript render partial: 'artist-work-entity', locals:{entity: @entity} %></div>")

NProgress.done();
