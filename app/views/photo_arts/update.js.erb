var id = "#<%= @id %>"
$(id).html("<%= escape_javascript render partial: 'artists/artist-work-entity', locals:{entity: @photo_art} %>")
$.modal.close();
$('#add-work-entity-modal').empty();

NProgress.done();
