var id = "#<%= @id %>"
$(id).html("<%= escape_javascript render partial: 'artists/artist-work-entity', locals:{entity: @production} %>")
$.modal.close();
$('#add-work-entity-modal').empty();

NProgress.done();
